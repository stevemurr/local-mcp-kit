name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-minimum-toolchain:
    name: Build (Swift 6.0 floor)
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Select minimum Swift toolchain
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.0"

      - name: Show Swift version
        run: swift --version

      - name: Describe package
        run: swift package describe

      - name: Build
        run: swift build

      - name: Build (release)
        run: swift build -c release

  test:
    name: Test (default toolchain)
    runs-on: macos-15
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build
        run: swift build --build-tests

      # Each target runs serially in its own bounded step: the raw-socket
      # suites block cooperative-pool threads while their peers answer, and
      # the hosted runners have wedged whole unbounded invocations. A wedged
      # target times out without hiding the results of the others.
      - name: Test LocalMCPContractsTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPContractsTests

      - name: Test LocalMCPDiscoveryTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPDiscoveryTests

      - name: Test LocalMCPDiscoveryBonjourTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPDiscoveryBonjourTests

      - name: Test LocalMCPProducerTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPProducerTests

      - name: Test LocalMCPConsumerTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPConsumerTests

      - name: Test LocalMCPIntegrationTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPIntegrationTests

      - name: Test LocalMCPTwoProducerExampleTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPTwoProducerExampleTests

      - name: Test LocalMCPCommandTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPCommandTests

      - name: Test LocalMCPSeparateProcessTests
        if: ${{ !cancelled() }}
        run: gtimeout -k 15 420 swift test --no-parallel --filter LocalMCPSeparateProcessTests
